[ ! "$DAXDEV" ] && echo "DAXDEV is not set, skipped this test group." && return 1
[ ! -e "/dev/$DAXDEV" ] && echo "device file /dev/$DAXDEV not exists, skipped this test group." && return 1

dir_cleanup() {
	true
}
