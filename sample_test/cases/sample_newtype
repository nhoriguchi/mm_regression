_control() {
		true;
}

_prepare() {
		true;
}

_cleanup() {
		true;
}

_check() {
		count_testcount "testcase $(basename $BASH_SOURCE)"
		count_success pass
		FALSENEGATIVE=true
		count_testcount "testcase $(basename $BASH_SOURCE)_2"
		count_failure fail
		FALSENEGATIVE=false
}
